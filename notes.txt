O que são componentes no React Native:
    dentro do App.js, tal irá retornar uma função que irá retornar um XML
    O <View> </View> é um componente proprio do react native, já o status Bar, é um componente do expo;
    E podemos criar nossos proprios componentes sendo eles peças que encaixamos e montamos a aplicação;

    sempre importar o react;

    O React Native sempre precisa utilizar componentes natiovs para ler, não apenas inserir o texto fara com que leia mas é necessario um componente que possua um texto;

    <Image source={parametro importado} /> /* Importado do react-native */

    Dimensions 
    Dimensions.get('screen').width; // busca o tamanho da tela
    /* o correto é atribuir a uma constante que busca as informações do tamanho da tela e é importado do react-native */

    /* 
    Se for para posicionar a imagem por completo o correto é dividir a largura e a altura da imagem e multiplicar pelo tamanho da tela
     */

     FlatList /** Passando data, definir a renderização de items */

     /** MOCKS 
        Informações ou funções para substituir os dados originais;
        geralmente utilizados em textes automatizados, para substituir as funções originais para não afetar os dads reais da aplicação;
     */